# Doppler에서 주입 받는게 아닌 테스트용 API-KEY
INTERNAL_MASTER_KEY: "aj39sdfkjasd93jkflasjf93jkflasj93"
INTERNAL_API_KEY: "8Gf21f93jsla81fj29ff3kssl29s8fa1"

spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MariaDB;DB_CLOSE_DELAY=-1;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect   # 변경
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  kafka:
    listener:
      auto-startup: false
    topic:
      topics:
        reservation-created: reservation-created-topic
        reservation-updated: reservation-updated-topic

  data:
    redis:
      host: 0
      port: 0

  flyway:
    enabled: false
